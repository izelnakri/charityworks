{{yield (hash
  isLoading=isLoading
  model=model
  showErrors=showErrors
  checkbox=(component 'form/cw-checkbox' form=this)
  input=(component 'form/generic-input' form=this)
  textarea=(component 'form/cw-textarea' form=this)
)}}
